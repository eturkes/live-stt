# Virtual environment
.venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/

# Build artifacts
dist/
build/

# Tool caches
.ruff_cache/
.pytest_cache/

# Bench corpus WAVs (gitignored; the replay.py / test regression corpus, D-014)
spike/backends/cache/

# Local STT model weights (download via models/README.md; large binaries)
models/*
!models/README.md

# Claude Code per-user overrides (committed counterparts: .claude/settings.json, CLAUDE.md)
.claude/settings.local.json
CLAUDE.local.md

# .serena/ is committed as is via its own .serena/.gitignore; Serena memory disabled globally (D-013)

# Headroom tokensave code-graph state (regenerable via tokensave init/sync; deny-Read'd in .claude/settings.json)
.tokensave/

# Environment / secrets
.env
.env.*
*.key

# OS
.DS_Store
Thumbs.db
