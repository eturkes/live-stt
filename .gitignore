# Virtual environment
.venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/

# Build artifacts
dist/
build/

# Tool caches
.ruff_cache/
.pytest_cache/

# Bench corpus WAVs (gitignored; the replay.py / test regression corpus, D-014)
spike/backends/cache/

# Local STT model weights (download via models/README.md; large binaries)
models/*
!models/README.md

# Claude Code per-user overrides (project settings.json IS committed)
.claude/settings.local.json

# Serena/Headroom LSP state (project.yml is tracked; .agent/ is the sole memory store — D-013)
.serena/memories/

# Environment / secrets
.env
.env.*
*.key

# OS
.DS_Store
Thumbs.db
