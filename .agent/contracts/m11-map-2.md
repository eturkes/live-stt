# map-m11-2 — M11.3 remainder → per-deliverable unit specs

Fill `answer` + `evidence` per row. Grade: `python3 .scratch/check_map.py .scratch/agents/map-m11-2.md`.
`evidence` = `file:line`, a command + its real output, or a commit SHA. Never a claim without a pointer.

## D2 — VAC evaluator port

### A1 — House pattern in `tests/eval_models.py`: name the functions + line ranges implementing (i) deterministic content scoring, (ii) timing/RSS measurement held out of the content hash, (iii) content-addressed detail cache, (iv) atomic aggregate write. Which are reusable verbatim for a VAC evaluator, which need a VAC-specific rewrite?
- answer: (i) `DecodeObservation` (`173-178`) declares deterministic `content` beside measured seconds; `_score` (`529-553`) consumes only `content`; `build_manifest` (`1407-1505`) publishes scored rows under `deterministic`. (ii) `run_worker` (`1611-1713`) keeps decode/wall/RSS in child `measurements`; `build_manifest:1494-1503` installs them as a sibling with `excluded_from_deterministic_equality=true`. (iii) `install_content_addressed_details` (`1508-1539`) validates the full SHA-256, derives `<model>-<sha[:16]>.jsonl`, collision-checks, and atomically installs. (iv) `install_evidence` (`1542-1560`) installs details first and the aggregate last through `write_atomic` (`tests/fetch_real_clips.py:172-185`, exclusive `.part`, flush+fsync, same-directory replace). Reuse `_json_bytes`, `write_atomic`, detail installation, file hashing, generic CER `_score`/aggregation, and process orchestration verbatim. Rewrite the observation/row validator and `build_manifest` for VAC event traces, arm×corpus identity, per-character lag, actual OpenVINO placement, and VAC-specific deterministic/timing fields; do not bend the offline model schema around them.
- evidence: `tests/eval_models.py:173-178,374-385,529-553,1407-1560,1611-1713`; `tests/fetch_real_clips.py:172-185`.

### A2 — `tests/eval_streaming.py` schema-2 resume/atomicity: what resumes, which key identifies an already-completed row, and does a VAC port reuse that schema or need a new one?
- answer: The full ordered corpus JSONL resumes, not an arbitrary set: `.part`, paired `.measurements.part`, and atomically-written `.resume.json` carry deterministic rows, timing rows, and the content identity. Restart accepts only a state exactly equal to `{adapter,engine,inputs,schema_version}`; it validates each prefix row against the case at the same index, where `case_id` is the row key, truncates both journals to their common complete prefix, then appends+fsyncs detail before measurement. A crash between fsyncs re-decodes one row. Only full paired coverage renames `.part` to the immutable detail. VAC should reuse these helpers/mechanics, but needs its own schema/version and a unique ordered composite row id such as `arm_id/corpus_id`; `streaming_baseline`'s event/accounting contract is not the VAC contract.
- evidence: `tests/eval_streaming.py:50,656-702,705-846,1121-1131`; identity is `{adapter,engine,inputs,schema_version}` and row validation requires `row.case_id == cases[index].case_id`.

### A3 — Which `.scratch/` producer is the authoritative shipped-policy harness to port? Rank `policy_vac.py`, `streaming_policy.py`, `compare_policies.py`, `policy_baseline.py`, `policy_corpus.py` by fidelity to the shipped VAC path, and report each one's real import health at HEAD (actually import it under the project venv; a parse is not enough).
- answer: Fidelity rank: (1) `policy_vac.py` = intended VAC arm (real silero, cadence, tail flush), but it drives a copied scratch `StreamingProcessor`, not production `_vac_segments`; port its arm/schema intent, then make production `replay.worker` authoritative. (2) `policy_baseline.py` = real replay worker/VAD/ring/chunker, but VAD-close policy and a `_decode` override, not VAC. (3) `streaming_policy.py` = LocalAgreement algorithm + computational clock, but no silero controller. (4) `policy_corpus.py` = exact pinned-input selector only. (5) `compare_policies.py` = post-hoc lag/scoring only. Clean argument-free import health: `streaming_policy`, `compare_policies`, `policy_corpus` YES; `policy_vac`, `policy_baseline` NO because argparse executes at import and exits 2 for missing `device`. Thus no scratch producer is safe to import verbatim; move executable bodies behind `main()` or port behavior into a committed evaluator that calls production replay.
- evidence: `UV_PROJECT_ENVIRONMENT=.venv env -u PYTHONPATH uv run --no-sync python -c <importlib loader with sys.argv=["import-check"]>` → `policy_vac rc=2 ... required: device`; `streaming_policy OK rc=0`; `compare_policies OK rc=0`; `policy_baseline rc=2 ... required: device`; `policy_corpus OK rc=0`. Producer mechanics: `.scratch/policy_vac.py:43-171`; `.scratch/policy_baseline.py:31-129`; `.scratch/streaming_policy.py:40-379`; `.scratch/policy_corpus.py:13-22`; `.scratch/compare_policies.py:17-86`.

### A4 — Layer separation: assign all 12 `.scratch/` producers to exactly one of {VAD/VAC policy runs · prompt-conditioning matrices · earlier whole-file device probes}, and state which layer the port must canonize and which it must not.
- answer: L1 VAD/VAC policy = `streaming_policy.py`, `policy_vac.py`, `policy_baseline.py`, `policy_corpus.py`, `compare_policies.py`, `decode_cost_matrix.py`. L2 prompt conditioning = `prompt_hotword_matrix.py`, `npu_asr_prompt_hotword_matrix.py`. L3 earlier whole-file/device probes = `long_form_whisper_eval.py`, `whisper_eval.py`, `whisper_probe.py`, `vad_real_compare.py`. Canonize L1 as the evaluator's arm/corpus/metric intent while replacing copied decode policy with the current production replay path. Keep L2 as a separately-labelled provenance family for inventory rows; do not make its oracle/prev-text probes look like shipped-policy rows. Do not canonize L3: it bypasses the shipped VAC worker or only probes component/device behavior and is superseded as end-to-end evidence.
- evidence: `.scratch/agents/map-m11u3-2.md:18-29`; `.scratch/whisper_eval.py:58-91`; `.scratch/whisper_probe.py:10-38`; `.scratch/vad_real_compare.py:11-14,47-72`; `.scratch/prompt_hotword_matrix.py:12-22`; `.scratch/npu_asr_prompt_hotword_matrix.py:12-22`.

### A5 — Exact CLI surface the ported evaluator must expose to match the house pattern (aggregate-only, resume, detail cache, corpus pinning). Give the flag set as it exists in the two committed evaluators.
- answer: `eval_models`: public default full run plus `--aggregate-only`; hidden child flags `--worker {model}`, `--details PATH`, `--summary PATH`. `eval_streaming`: the same four flags plus optional positional `variants...` (`560ms` default, named variants or `all`), forbidden with aggregate-only. Neither exposes `--resume`, a cache path, or a corpus path: row resume is automatic from fingerprinted `.part/.measurements.part/.resume.json`; details install to evaluator-owned content-addressed paths; corpus manifests/paths are committed constants. VAC should expose default full run + `--aggregate-only`, hidden `--worker/--details/--summary`, and at most a public arm selector analogous to `variants`; it must keep the two pinned corpora internal and automatic resume implicit. A user-facing `--out`, arbitrary WAV, cache-dir, or resume flag would break the house contract.
- evidence: `tests/eval_models.py:2019-2041`; `tests/eval_streaming.py:1541-1571`; automatic resume `tests/eval_streaming.py:656-846`.

### A6 — Can aggregation, schema, resume and atomicity be proved on a synthetic corpus with no NPU and no model? Name the seam a fake recogniser drives (cite M11.2's fake-pipeline technique in `tests/test_shipped_path.py`) and any hard blocker.
- answer: YES; no hard blocker. M11.2 injects `_FakePipeline` as `openvino_genai.WhisperPipeline`, instantiates the real `WhisperEngine`, makes `generate()` return deterministic text/timestamp chunks as a function of sample count, and drives the real `_vac_segments` with `_StubVad` through `run_vac`. A two-or-more-row synthetic corpus can drive that same real-engine/fake-binding seam for VAC content/event validation. The evaluator's row writer can then receive the fake decode callable directly to test schema rejection, prefix reconciliation, paired fsync resume, content-addressed detail install, and aggregate-last atomicity without spawning hardware workers. Subprocess launch remains separately testable by a tiny fake worker script/monkeypatched runner; hardware placement is a live M11.5 check, not needed to prove machinery.
- evidence: `tests/test_shipped_path.py:51-98,380-444,447-468`; reusable resume/install seams `tests/eval_streaming.py:672-846`, `tests/eval_models.py:1508-1560`, `tests/fetch_real_clips.py:172-185`.

### A7 — Estimated shipped size for the port: new evaluator line count + test line count, with the comparable committed artifact you sized against.
- answer: Planning point **900 evaluator LOC + 600 test LOC**; credible range 800-1,000 + 500-700. The nearest committed artifact is `eval_streaming` at 1,575 + 1,053 LOC: VAC reuses its generic fingerprint/serialization/resume/install/process machinery and has only two corpora plus a small fixed arm set, so it should be about 55-60% of that surface. The six L1 scratch producers total 894 LOC, but 379 lines are the now-production `StreamingProcessor` copy and much orchestration is non-atomic; replacing that copy with production replay while adding the house kernel lands near 900, not near a 200-line script port. Size this as its own full kernel unit.
- evidence: `wc -l` → `tests/eval_streaming.py 1575`, `tests/test_eval_streaming.py 1053`; L1 scratch = `policy_vac.py 171 + streaming_policy.py 379 + policy_baseline.py 129 + decode_cost_matrix.py 91 + compare_policies.py 102 + policy_corpus.py 22 = 894`.

### A8 — Does the port add `from live_stt import …` seeds that extend `asr_contract_sha256`'s closure? List them and state the contract/cut consequence.
- answer: It must **not** extend the existing `asr_contract_sha256`; that hash is intentionally the sherpa offline-row contract. A minimal VAC evaluator will need `ASR_DEVICE`, `VAC_CHUNK_S`, `VAC_TRIM_S`, `check_models`, and `load_recognizer` (then drive `replay.replay_recognizer`); `check_models/load_recognizer` are existing seeds, while the first three and the closure-reached `WHISPER_ENGINES`, `WhisperEngine`, `_vac_segments`, `OPENVINO_CACHE_DIR` (plus `ASR_HOTWORDS_DEVICES` if a hotword arm exists) are current declared cuts. `ASR_CONTRACT_SEEDS` is a fixed tuple, not an import scanner, so a new evaluator import silently changes nothing unless code edits that tuple. Correct design: leave seeds/cuts unchanged and add a separate VAC contract hash whose roots include those VAC/Whisper names and whose dependency surface includes `streaming.py`; otherwise removing the cuts makes every VAC edit invalidate the sherpa baseline and recreates the just-fixed false-refusal class.
- evidence: `tests/eval_models.py:914-972` (fixed seeds + declared cuts), `:975-1038` (closure/hash); `tests/test_eval_models.py:580-628` locks `reachable == contract | cuts` and proves `_vac_segments`/`WhisperEngine` edits irrelevant to the sherpa hash.

## D3 — D-016 claim inventory + validator

### B1 — Complete enumeration of every D-016 numeric claim: one line per claim as `metric · value · producing artifact or NONE`. Sources: `.agent/memory.md` D-016, commit bodies `1cc241a` `23e8fa8` `b633b0c` `76d986d`, `README.md`, `.agent/roadmap.md`. Report the total count.
- answer: **40 claim rows** (C34 in the source map is an audit sentinel, not a claim):
  1. long_form reference characters · `N=1383` · `.scratch/policy_long_form_vac_npu.json:N`
  2. retention duration/reference · `182 s`, `N=1166` · `.scratch/policy_retention_vac_npu.json:audio_s=182.482,N`
  3. pause-free silero segmentation · `8 segments`, `20-33 s` · `.scratch/policy_results.md:45`
  4. pause-free VAD lag median · `15.5 s` · `.scratch/policy_results.md:32` (`15.504`)
  5. pause-free VAD lag max · `36.6 s` · `.scratch/policy_results.md:32` (`36.587`)
  6. pause-free VAC lag median · `2.5 s` · `.scratch/policy_results.md:28` (`2.483`)
  7. pause-free VAC lag max · `8.1 s` · `.scratch/policy_results.md:28` (`8.129`)
  8. paused pure-streaming lag median range · `3.18-3.74 s` · `.scratch/policy_results.md:17,19,21` (`3.176,3.306,3.742`)
  9. paused plain-VAD lag median · `2.43 s` · `.scratch/policy_results.md:19` (`2.430`)
  10. long_form before CER · `0.2538` · `.scratch/policy_long_form_vad_k2v2.json:cer`
  11. long_form after CER · `0.2321` · `.scratch/policy_long_form_vac_npu.json:cer`
  12. retention before CER · `0.1587` · `.scratch/policy_retention_vad_k2v2.json:cer`
  13. retention after CER · `0.0583` · **NONE** (closest `.scratch/policy_retention_vac_npu.json:cer=0.0686106346483705`)
  14. retention before lag median · `14.239 s` · `.scratch/policy_results.md:34`
  15. retention after lag median · `2.483 s` · `.scratch/policy_results.md:28`
  16. k2v2 RTF · `0.041` · `.scratch/policy_results.md:22`
  17. Whisper VAC RTF range · `0.48-0.60` · `.scratch/policy_results.md:16,28` (`0.481,0.595`)
  18. real-time headroom · `1.7-2.1x` · derived from `.scratch/policy_results.md:16,28,72`
  19. NPU unconditioned long_form CER · `0.2321` · `.scratch/policy_long_form_vac_npu.json:cer`
  20. NPU unconditioned retention CER · `0.0686` · `.scratch/policy_retention_vac_npu.json:cer`
  21. GPU unconditioned long_form CER · `0.2292` · `.scratch/policy_lf_vac_gpu.json:cer`
  22. GPU unconditioned retention CER · `0.0695` · `.scratch/policy_ret_vac_gpu.json:cer`
  23. NPU prompt/hotword refusal · lengths `1,2,4,8,16,32,64,128,200` raise; `0` passes · `.scratch/policy_results.md:73-77`
  24. pause-free prev-text CER · `1.8919` · `.scratch/policy_ret_vad_gpu_prev.json:cer`
  25. pause-free unconditioned comparator CER · `0.1278` · `.scratch/policy_ret_vad_gpu.json:cer`
  26. prev-text insertions/reference · `I=2126`, `N=1166` · `.scratch/policy_ret_vad_gpu_prev.json:I,N`
  27. repeating tail · one clause `7x` · `.scratch/policy_results.md:57`
  28. prior streaming-buffer loop CER · `0.9328` · `.scratch/policy_arms.log:21` (`0.9327548806941431`)
  29. prior Kotoba loop CER · `0.8402` · `.scratch/long_form_kotoba_gpu.json:metrics.cer`
  30. paused prev-text change · `0.2408 -> 0.2054` · `.scratch/policy_vad_gpu.json:cer`; `.scratch/policy_lf_vad_gpu_prev.json:cer`
  31. paused bounded-term change · `0.2408 -> 0.1873` · `.scratch/policy_vad_gpu.json:cer`; `.scratch/policy_lf_vad_hot.json:cer`
  32. append-only insertion fix · `I 12 -> 0` · **NONE for post-fix 0**; pre-fix `.scratch/policy_retention_vac_npu.json:I=12`
  33. append-only CER fix · `0.0686 -> 0.0583` · **NONE for post-fix 0.0583**; pre-fix `.scratch/policy_retention_vac_npu.json:cer`
  34. long_form duration · `4:48` · `.scratch/policy_results.md:3`; `.scratch/policy_long_form_vac_npu.json:audio_s=288.521`
  35. relative decode cost · `~10x` · derived from `.scratch/policy_results.md:16,22,28,34` (`10.26-11.73x`)
  36. NPU forfeited capability classes · `1` · `.scratch/policy_results.md:70,73-75`
  37. independent prompt-loop reproductions · `3` · derived from `.scratch/policy_results.md:57-58`
  38. passively learned unseeded terms · `0` · `.scratch/policy_results.md:87`
  39. evidence clips per corpus · `1` · `.scratch/policy_results.md:98`
  40. RAPL `energy_uj` mode · `400` · `.scratch/policy_results.md:99`
- evidence: `.agent/memory.md:161`; `.scratch/agents/map-m11u3-2.md:65-106`; count command → `mapped_claim_rows=40`, ids `C1..C33,C35..C41`; commit-body cross-checks `1cc241a`, `23e8fa8`, `b633b0c`, `76d986d`; README repetitions `README.md:5,86`; M11.5 gap/requirement `.agent/roadmap.md:55`.

### B2 — Partition B1's claims into: reproducible by committed machinery today · needs the D2 port · needs new code that no unit yet owns.
- answer: Exhaustive 4/30/6 partition over B1's 40 rows. **Committed today (4):** `1,2,34,39` — the two committed manifests + `cer.normalize` + WAV geometry reproduce both corpus lengths/durations and show one clip per corpus. **Needs D2 (30):** `3-22,24-28,30-33,35` — every L1 VAD/pure-stream/VAC arm result, device comparison, lag/CER/RTF/headroom derivation, conditioning arm run, and current append-only result. Retained artifacts can source historical halves; D2 supplies the rerunnable current measurement path. **Unowned new code (6):** `23` full NPU prompt-length matrix (L2); `29` Kotoba whole-file reproduction (L3, explicitly superseded); `36` exhaustive “exactly one feature” capability census; `37` three-way reproduction count because it depends on L3; `38` passive-learning unseeded-term measurement; `40` host RAPL permission/energy probe. D2 must not absorb these six merely to make the inventory green; assign, retain-as-legacy, or narrow/correct their claims explicitly.
- evidence: committed recomputation command (`tests/long_form.json`, `tests/retention_probe.json`, `cer.normalize`, WAV headers) → `long_form N=1383 audio_s=288.5210625`; `retention N=1166 audio_s=182.482`; layer/source map `.scratch/agents/map-m11u3-2.md:18-29,65-106`; D2 scope contract `.scratch/agents/contract-m11u3.md` V1-V4.

### B3 — Inventory artifact shape + validator hook: what the machine-readable inventory looks like, and where its validator runs (own `gate.py` step / inside an evaluator / a test). Name the house precedent it follows.
- answer: Commit `tests/d016_claims.json` as `{schema_version,claims:[...]}`. Each row carries `id`, verbatim `claim`, typed `claim_value`, `metric`, `corpus`, `arm`, `device`, tokenized `evaluator_key_path`, and `legacy_source:{status,path,key,stored_value}` where `status` includes `absent`; keep future `measured_value` out of the inventory so the claim is not mistaken for evidence. Add importable CLI `tests/validate_d016_claims.py INVENTORY EVIDENCE`: validate row/schema/unique ids, resolve every key path in evidence, and fail when the resolved value or its source/provenance is absent; never compare measured value to `claim_value` as truth. In D3, run it through a dedicated test with all-unmeasured (nonzero) and fully-populated (0) fixtures, under the existing blocking pytest gate. Do **not** add a direct blocking `gate.py` step while C13 is intentionally unmeasured and M11.5's baseline does not exist. M11.5 should add the direct validator step or invoke the same validator during evaluator finalization once the committed evidence is complete. House precedent: data manifest + importable validator + lock tests (`short_corpus.json`/`fetch_real_clips.py`/`test_corpus.py`, and `model_baseline.json`/`eval_models.py`/`test_eval_models.py`).
- evidence: acceptance `.scratch/agents/contract-m11u3.md` D3 C1-C5 + S3; blocking pytest inventory `gate.py:30-52`; baseline-lock precedent `tests/test_eval_models.py:426-548`; structural manifest precedent `tests/short_corpus.json`, `tests/fetch_real_clips.py`, `tests/test_corpus.py`.

### B4 — Estimated shipped size for the inventory + validator, with the comparable committed artifact you sized against.
- answer: Planning point: **420 JSON lines (~20 KiB) + 160 validator Python lines + 180 test lines = ~760 lines total**; credible range 650-900. Forty rows × roughly 9-10 pretty-JSON lines dominates the data artifact. The validator is materially smaller than `fetch_eval_models.py` because it resolves typed key paths rather than acquiring archives; its tests are comparable to `test_eval_model_acquisition.py`. Size D3 as its own kernel unit despite the data file: the validator and the claim-vs-measurement semantic boundary carry judgment.
- evidence: comparable counts: `tests/short_corpus.json 161 lines/5,464 B`, `tests/retention_probe.json 190/8,038 B`, `tests/fetch_eval_models.py 434`, `tests/test_eval_model_acquisition.py 161`; B1 fixes the inventory cardinality at 40.

## D4 — Whisper replay golden

### C1 — `tests/gen_replay_goldens.py` matrix structure: what one row is, how a golden is keyed, and exactly what adding a whisper/VAC row requires.
- answer: The matrix is currently a Cartesian product: each `ENGINES` member × every `all_clips()` tuple `(clip_id, ja_ref, purpose)`. One golden row is `{ja_ref, purpose, n_segments, segments:[{start,n,text}]}`, keyed `golden[engine][clip_id]`; timing/RTF are deliberately discarded. Adding `"whisper"` to `ENGINES` would regenerate a Whisper/VAC row for every available clip, because `replay_wav(...,"whisper")` reaches the worker's shipped duck-dispatched VAC path. A single shortest-clip lock therefore needs a small matrix refactor (explicit `(engine, clip)` cases or a Whisper clip allowlist), plus `"whisper"` in the committed JSON and its model-gated regression case; merely extending `ENGINES` creates 12 Whisper rows on this checkout, not one.
- evidence: `tests/gen_replay_goldens.py:36,63-72,81-101`; `replay.py:7-10,92-110,168-174`; `tests/replay_goldens.json` currently has 2 engines × 12 clips.

### C2 — Does `replay.py --engine whisper` run end to end today? Report the exact command, its rc, and its output.
- answer: YES on the exact NPU target. Command: `source /run/host/home/eturkes/.local/app/intel-accel/env.sh; UV_PROJECT_ENVIRONMENT=.venv env -u PYTHONPATH uv run --no-sync python replay.py spike/backends/cache/greet.wav --engine whisper --json`. `rc=0`; output: `engine=whisper`, `audio_s=0.8509375`, `n_segments=1`, `n_nonempty=1`, segment `{start:1280,n:12335,seg_len:12335,text:"こんにちは"}`; measured `decode_s=0.6633998749998682`, overall RTF `0.7796105765698047`.
- evidence: command above → `RC=0` with the reported JSON; CLI/path anchors `replay.py:92-110,168-174,202-209`.

### C3 — Determinism risk that decides whether a byte-exact whisper golden is possible at all: decode the shortest pinned clip twice through the shipped whisper path and diff the hypotheses. Report both runs and the diff. A blocker (missing model, missing NPU farm) is a valid answer — report it with the failing command, do not fight it past ~15 tool calls.
- answer: ACHIEVABLE on the sampled shortest clip: two independent project-venv Python processes loaded the exact NPU path and produced the same deterministic row. Run 1 `rc=0 [{"n":12335,"start":1280,"text":"こんにちは"}]`; run 2 identical; unified diff `<empty>`. This proves the requested two-run regeneration probe for `greet.wav`; it does not establish cross-driver or cross-device stability. The lock must exclude `decode_s`/RTF, as existing goldens do.
- evidence: `source /run/host/home/eturkes/.local/app/intel-accel/env.sh; run='import json,replay; r=replay.replay_wav("spike/backends/cache/greet.wav","whisper"); print(json.dumps([{"start":s["start"],"n":s["n"],"text":s["text"]} for s in r["segments"]],ensure_ascii=False,sort_keys=True))';` then invoke `UV_PROJECT_ENVIRONMENT=.venv env -u PYTHONPATH uv run --no-sync python -c "$run"` twice → both rc 0, both `[{"n": 12335, "start": 1280, "text": "こんにちは"}]`, `DIFF <empty>`.

### C4 — Estimated shipped size for the whisper golden row + its regeneration lock.
- answer: Planning point **~100 changed/added lines**: 15-20 JSON lines for one `whisper/greet` row, 30-40 generator lines to replace the engine×all-clips Cartesian product with an explicit matrix/canonical byte renderer, and 40-55 test lines for resource gating plus `run1 == run2 == committed` on `{n_segments,segments[{start,n,text}]}`. Credible range 80-130. Keep timing out of all three comparisons. This is a small independent kernel unit; the NPU decode time is operational cost, not code size.
- evidence: current surface = `tests/replay_goldens.json 364 lines/10,673 B/24 rows`, `tests/gen_replay_goldens.py 111`, `tests/test_replay.py 130`; sampled row is one segment. C3 command produced identical deterministic bytes twice, so no fallback design is required.

## Cross-cutting

### D1 — Dependency order among D2/D3/D4, with the reason per edge. State explicitly whether the claim inventory can be built before the evaluator port.
- answer: Required DAG edge: **D3 -> D2**. Build the inventory first: it fixes all 40 claim identities and the intended evaluator key paths, then D2 implements those paths and cannot accidentally omit an arm/metric. Rows may carry `legacy_source.status=absent` and unresolved future paths, so no measured baseline is needed to commit D3. **Yes, the claim inventory can and should be built before the evaluator port.** D4 has no code dependency on either: replay already reaches shipped VAC and C3 proved the shortest row deterministic. Recommended queue is D3, D2, D4 because that follows information value and leaves the smallest self-contained lock last; D4 may run in parallel or before D2 without rework. M11.5 depends on both D3 and D2; it does not depend on D4.
- evidence: deliverable order/acceptance `.scratch/agents/contract-m11u3.md` (`D1 -> D3 -> D2 -> D4`, D3 C1-C5, D2 V1-V4); M11.5 consumes evaluator + fully populated inventory `.agent/roadmap.md:55`; C2-C3 show D4 is runnable independently today.

### D2x — Per deliverable, the gate identity change it forces: new `gate.py` step, new test file, changed blocking-step count, or none.
- answer: **D2:** add `tests/test_eval_vac.py`; exercise the synthetic multi-row evaluator through existing blocking `pytest`; no direct evaluator step because D2 commits no real baseline. **D3:** add `tests/test_d016_claims.py` (and validator module/data); fixture-grades run under `pytest`; no direct validator step while the known row is unmeasured. **D4:** extend `tests/test_replay.py` and existing generator/golden; no new test file required. All three leave `gate.py` at **7/7 blocking steps**. M11.5, not these units, must add one blocking VAC aggregate/inventory-validation step after real evidence exists; combining both checks in one evaluator step avoids two redundant gate launches.
- evidence: current seven-step identity `gate.py:50-63`; current green identity `.agent/memory.md:59`; D2 no-real-baseline clause `.scratch/agents/contract-m11u3.md` V4; D3 known absence `.scratch/agents/contract-m11u3.md` C4; existing replay tests `tests/test_replay.py:80-130`.
