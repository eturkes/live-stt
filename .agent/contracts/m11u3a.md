# M11.3a acceptance contract (MAIN-authored)

Unit = D-016 claim registry + omission validator. `tier=kernel`. No hardware, no production change.
Supersedes `m11u3.md` § D3 (`C1`–`C5`) by refining it; `S1`–`S3` there still bind.
Every downstream artifact decides against this file.

## Why kernel

The registry is the specification M11.3d implements against and M11.5b populates; a wrong disposition
is never re-checked downstream — M11.6a consumes the registry as the single claim→source surface
(`m11-map-3.md` G2), so a mis-ruled row silently becomes a shipped doc claim.

## Shipped artifacts

| path | role |
|---|---|
| `tests/d016_claims.json` | the registry: `{schema_version, claims:[40]}` |
| `tests/validate_d016_claims.py` | importable validator + CLI `INVENTORY EVIDENCE` |
| `tests/test_d016_claims.py` | locks: registry shape, disposition census, both-ways fixture grading, validator probes |
| `tests/d016_fixture_unmeasured.json` | inventory fixture, every row unresolved ⇒ nonzero |
| `tests/d016_fixture_complete.json` | inventory fixture, every row resolved ⇒ zero |
| `tests/d016_fixture_evidence.json` | the evidence both fixtures are graded against |

## Row schema (strict — an unknown key is an error)

| field | type | rule |
|---|---|---|
| `id` | str | `^C[0-9]+$`, unique across the file |
| `claim` | str | verbatim D-016 claim text, non-empty |
| `claim_value` | number \| str \| bool \| list \| object | non-null; the claim as data |
| `metric` | str | non-empty token (`cer`, `lag_median_s`, `rtf`, `reference_chars`, …) |
| `corpus` | str \| null | `long_form` \| `retention` \| null |
| `arm` | str \| null | `vac` \| `vad` \| `stream` \| `vad_prev` \| `vad_hotwords` \| `whole_file` \| null |
| `device` | str \| null | `NPU` \| `GPU` \| `CPU` \| null |
| `disposition` | str | `measured` \| `corrected` \| `narrowed` \| `legacy-retained` \| `absent` |
| `evaluator_key_path` | list[str] \| null | tokens walked against EVIDENCE |
| `replacement` | object \| null | `{claim: str, source: str}` |
| `legacy_source` | object | `{status: "present"\|"absent", path, key, stored_value}` |
| `note` | str \| null | free text; **required non-empty** when `legacy_source.status == "absent"` |

**No row carries a measured value.** A key named `measured_value` (or any key outside the table) is a
structural error, so a claim can never be mistaken for evidence (`m11u3.md` C5).

## Disposition rules — the determinate pass condition

| disposition | `evaluator_key_path` | `replacement` | extra |
|---|---|---|---|
| `measured` | non-empty list | null | path must resolve in EVIDENCE to a non-null value |
| `corrected` | null | both fields non-empty | states the superseded value in `claim`, the new one in `replacement` |
| `narrowed` | null | both fields non-empty | replacement is the defensible weaker claim |
| `legacy-retained` | null | null | `legacy_source.status == "present"` with `path`, `key`, `stored_value` all non-null |
| `absent` | null | null | transitional; **always an error** — M11.5b must clear every one |

`legacy_source.status == "present"` ⇒ `path`, `key`, `stored_value` all non-null.
`legacy_source.status == "absent"` ⇒ `path`, `key`, `stored_value` all null **and** `note` non-empty.

## Validator contract

`tests/validate_d016_claims.py`, importable, three public entry points:

- `load(path) -> dict` — parse; raise on unreadable/malformed JSON.
- `validate_structure(inventory) -> list[str]` — schema, ids, per-disposition required fields. Evidence-free.
- `validate(inventory, evidence) -> list[str]` — `validate_structure` **plus** resolution and omission.

Error strings are stable and id-prefixed (`C13: …`) so a test can assert on the offending row.

CLI: `python tests/validate_d016_claims.py INVENTORY EVIDENCE` → prints one error per line, exits `1`
when any error, `0` when none.

Key-path resolution walks tokens against EVIDENCE: a token indexes a dict key, or a list position when
it is all digits. A missing key, an out-of-range index, a non-indexable node, or a resolved `null` is a
resolution error. The validator **never compares a resolved value to `claim_value`** — it asserts
presence and sourcing, never truth (`m11u3.md` C5).

## Predicates

- `P1` The registry carries exactly **40** rows with unique ids `C1`–`C33` and `C35`–`C41`. `C34` is the
  source census's audit sentinel, not a claim, and is deliberately absent — a test names the gap so a
  later renumber cannot silently close it.
- `P2` Disposition census is exactly: `measured` 31 · `narrowed` 4 · `legacy-retained` 1 · `corrected` 1 ·
  `absent` 3. A committed test asserts each count and the exact id set per non-`measured` disposition.
- `P3` The three `absent` rows are exactly `C13`, `C32`, `C33` — retention CER `0.0583`, the post-fix
  `I=0`, and the post-fix `0.0583`. Each carries a `note` recording that an exhaustive `.scratch/`
  search found no producer artifact and naming the closest pre-fix one.
- `P4` The six rows no unit measures carry their ruled disposition: `C23` narrowed · `C29`
  legacy-retained · `C37` narrowed · `C38` narrowed · `C39` narrowed · `C41` corrected.
- `P5` `validate_structure(registry) == []` — the shipped registry is structurally clean today.
- `P6` Graded both ways at seed: `validate(unmeasured_fixture, evidence)` is non-empty and the CLI exits
  `1`; `validate(complete_fixture, evidence) == []` and the CLI exits `0`. Both fixtures are committed.
- `P7` Every `legacy_source` with `status == "present"` resolves in the real artifact: a committed test
  walks each `(path, key)` and requires the stored value to match, **skipping** when `.scratch/` is
  absent (gitignored, so a fresh clone must not fail). The skip is per-row and reported.
- `P8` Adversarial validator probes each produce an error naming the offending id: duplicate id, unknown
  key, missing required field, wrong `disposition`, `measured` with a null path, `measured` with a path
  that does not resolve, `measured` resolving to `null`, `corrected`/`narrowed` with an empty
  replacement, `legacy-retained` with a null `stored_value`, `absent` with a blank `note`, a
  `measured_value` key, a bad `schema_version`.
- `P9` `gate.py` stays **7/7** blocking. The validator runs under the existing pytest step; no direct
  gate step, because `C13`/`C32`/`C33` stay deliberately `absent` until M11.5b (`m11-map-2.md` B3).

## MAIN-derived corrections to the source census (`m11u3-map-2.md`)

Re-derived this unit against the live `.scratch/` tree; 22/23 JSON values and 20/20 line anchors confirmed.

- **`C29` pointer defect.** The census records key path `metrics.cer` in `.scratch/long_form_kotoba_gpu.json`;
  no `metrics` key exists. The real path is **`vad_prev.cer`**, value `0.8402024584237165` (unchanged).
  The registry carries the corrected pointer; `P7` is what catches this class.
- **`C13`/`C32`/`C33` absence re-confirmed by MAIN.** Literal `0.0583` appears only under
  `.scratch/agents/` planning reports, never in a producer JSON/MD/log; positive control found
  `0.0686106346483705` in `policy_retention_vac_npu.json` and `vac_npu.log`.

## Out of contract

Populating any row (M11.5b) · the VAC evaluator that supplies the key paths (M11.3b/M11.3d) · a
human-facing rendering of the registry (M11.6a) · a direct `gate.py` validator step (M11.5a) ·
production edits (`m11u3.md` S1).
