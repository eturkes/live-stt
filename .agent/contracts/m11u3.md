# M11.3 acceptance contract (MAIN-authored)

Unit = VAC evaluator machinery + fingerprint repair. `tier=kernel`. Machinery only — **no committed real baseline**.
Every downstream artifact decides against this file.

## Deliverable order (highest-value first; each independently committable)

`D1` fingerprint repair → `D3` D-016 claim inventory + validator → `D2` VAC evaluator machinery → `D4` whisper replay golden.

---

## D1 — `pipeline_fingerprint()` narrowed to a dependency-scoped ASR contract

### Measured ground truth (MAIN, this session)

- Baseline bytes last written by **`e2a8d9c`** (`eval (M10.4)`). Stored `live_stt.py` sha256 `7a25cece…08be`, 39,802 B, 1,018 lines. HEAD = `4857af96…1497`, 1,465 lines.
- AST closure from the real import seeds (`replay.py` + `tests/eval_models.py` `from live_stt import …`) = **42 top-level symbols**; **36** once `_vac_segments` + `WhisperEngine` are cut.
- Against `e2a8d9c`, that 36-symbol closure is **26 unchanged / 8 changed / 2 new**.
- **All 8 changes are additive whisper/VAC/context branches or a pure `ruff format` rewrap** — audited per symbol:
  - `ENGINE_DIRS` +`"whisper"` key; sherpa entries byte-identical.
  - `State` +`self.partial = ""`; a meter field, no decode effect.
  - `_decode` +`isinstance(rec, WhisperEngine)` early return; sherpa body byte-identical.
  - `check_models` marker selection by engine; `tokens.txt` preserved for sherpa.
  - `load_recognizer` +`device` param +whisper early return; sherpa branches byte-identical.
  - `worker` +`context=None` +`hasattr(rec, "decode_segments")` early return; sherpa path byte-identical.
  - `_decode_segments` +`context=None` +`context.observe_ja(text)` under `if context is not None`; the evaluator passes no context, so it is dead there.
  - `_split_decode_segment` pure `ruff format` rewrap of one slice expression (M11.1 convergence); semantically identical.
- **Behavioural proof the sherpa decode path is unchanged: `tests/test_replay.py` = 29 passed at HEAD** against committed goldens with real k2v2 + parakeet models — byte-exact hypotheses, engine-first keyed. This is committed, rerunnable evidence, not a diff opinion.

### Predicates

- `P1` Inclusion is **derived, exclusion is declared.** The contract source = AST closure over `live_stt.py` from declared seeds, minus a declared cut map `{name: reason}`. No hand-curated inclusion list.
- `P2` A committed test asserts `closure == contract ∪ cuts` exactly — no stale cut, no undeclared exclusion — so a new call into `live_stt` forces an explicit decision instead of silently entering or escaping the hash.
- `P3` An **irrelevant** `live_stt.py` edit leaves the fingerprint byte-identical. Proved in a throwaway tree on at least: a `_vac_segments` body edit, a `WhisperEngine` body edit, and one edit to a symbol outside the closure entirely.
- `P4` A **decode-input** edit changes the fingerprint. Proved on at least: `resample`, `_split_decode_segment`, `VAD_PRE_PAD_S`, and `_merge_chunk_text`.
- `P5` `_DECODE_MERGE_MAX_CHARS` and `SEGMENT_QUEUE_MAX` become fingerprinted. Both affect decoded text or run completion and neither appears in today's `values` block — the derived closure captures them automatically. A test names both.
- `P6` `python tests/eval_models.py --aggregate-only` exits 0 and reproduces `tests/model_baseline.json` **byte-exact**.
- `P7` The migration is **one idempotent replay from a clean base**: `git checkout e2a8d9c -- tests/model_baseline.json && python tests/eval_models.py --aggregate-only` reproduces the committed bytes. The permitting clause lives in `_pipeline_reaggregate_compatible` beside the two existing one-time clauses, carrying its justification in a comment, so the repair reruns from committed state (`CLAUDE.md` Engineering).
- `P8` Only the `deterministic.pipeline` provenance block moves. Every decoded row, detail sha256, corpus hash, model hash, VAD-model hash and `measurements` block stays byte-identical to `e2a8d9c`'s. A test asserts the row-bearing blocks are unmoved.

### Cut map + reason (each reason must state why it cannot change a decoded row)

| symbol | reason |
|---|---|
| `_vac_segments` | VAC branch; `worker` enters it only for a recogniser exposing `decode_segments`, which sherpa never does. |
| `WhisperEngine` | Whisper constructor; `load_recognizer` returns it only for `WHISPER_ENGINES`. |
| `ASR_DEVICE`, `WHISPER_ENGINES`, `ASR_HOTWORDS_DEVICES`, `OPENVINO_CACHE_DIR`, `VAC_CHUNK_S`, `VAC_TRIM_S` | Reached only through the whisper/VAC branches above. |
| `State` | Mutable counter bag (queue depths, drop counts, stop event) — measurement, never decoder input. Cutting it is also what keeps M11.4's force-trim counter from re-breaking the gate. |
| `emit_line`, `logger`, `_LINE_CLEAR`, `_STDOUT_TTY` | Presentation. Evaluator rows arrive through `replay.py`'s `on_segment` hook, not through stdout or the transcript. |
| `check_models` | Readiness preflight; raises or returns `None` and never reaches the decoder. |
| `ENGINE_DIRS`, `MODELS_DIR`, `VAD_MODEL` | Path constants. The bytes they point at are hashed independently by `model_fingerprint` and `pipeline["vad_model"]`. |

Everything else in the closure stays **in** the contract, including `load_recognizer` (it fixes `num_threads`/decoding method for sherpa) and `worker`/`_decode`/`_decode_segments` whole — conservative on purpose. Accepted limit: an edit to the dead whisper guard inside a contract symbol still invalidates. That is a false refusal, the safe direction, and it is recorded rather than engineered away with branch-aware slicing.

### Gate identity

`uv run --no-sync python gate.py` — 6/6 blocking, and the `aggregate-only` step **flips from non-blocking-red to green**. `gate.py`'s label for that step must stop calling it a known failure.

---

## D3 — Machine-readable D-016 claim inventory + validator

- `C1` One committed inventory file enumerating **every** D-016 claim, each row carrying: claim id, verbatim claim text, metric identity, corpus, arm/device, and the evaluator key path that will supply it.
- `C2` A validator that **exits nonzero when any inventory row has no measured value**, so an omitted metric fails loudly instead of yielding a byte-stable but incomplete baseline.
- `C3` Graded both ways at seed: the all-unmeasured inventory scores nonzero, a fully-populated fixture scores 0.
- `C4` Rows whose scratch provenance is absent are recorded as such in the inventory itself, not silently dropped. The known case is retention CER **0.0583**, which the roadmap says no artifact carries; the inventory records its status from measurement, and M11.5 re-derives or corrects D-016.
- `C5` The inventory is data, consumed by a kernel-tier validator; it never asserts a value is *true*, only that it is *present and sourced*.

---

## D2 — VAC evaluator machinery

- `V1` House pattern held: deterministic content separated from timing/RSS measurement, content-addressed detail, atomic aggregate, `SCHEMA_VERSION`.
- `V2` A **synthetic multi-row corpus proves aggregation, schema, resume/atomicity and validator behaviour with no hardware** — the whole machinery is exercised in the ordinary gate.
- `V3` The three scratch evidence layers stay separate and the superseded whole-file device-probe layer is **not** canonized.
- `V4` No committed real baseline. M11.5 runs it.

---

## D1..D4 shared

- `S1` No production behaviour change. `live_stt.py` and `streaming.py` are read-only this unit; if a deliverable seems to need a production edit, that is a finding for MAIN, not an edit.
- `S2` Every number written into a durable file traces to a command rerunnable from committed state.
- `S3` `python gate.py` green at close; `grep -nP '[\x{2013}\x{2014}]' README.md` clean if README is touched (L-021).
