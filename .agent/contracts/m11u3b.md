# M11.3b — VAC evaluator core: acceptance contract

`tier=kernel`. Deliverable = `tests/eval_vac.py` (house-pattern evaluator kernel for the shipped
whisper+VAC path) + `tests/test_eval_vac.py`, plus the closure-walker parameterization in
`tests/eval_models.py` that reaching `streaming.py` requires. Base SHA = `5f5872a`.

Scope boundary. This unit ships the KERNEL: schema + row identity, the VAC contract fingerprint,
serialization, automatic resume, content-addressed detail install, atomic aggregate, CLI. It does
NOT ship the arm × corpus matrix, the `measured` registry selectors, or the metric surface — those
are M11.3d. Every predicate below is proved on a synthetic multi-row corpus with **no NPU, no
model, no OpenVINO**, through M11.2's fake-pipeline seam (`_FakePipeline` injected as
`openvino_genai.WhisperPipeline`, real `WhisperEngine`, real `_vac_segments`, `_StubVad`) or a
fake decode callable passed to the row writer.

Dispatch inputs already run + committed — read, do not re-derive:
`.agent/contracts/m11-map-2.md` § D2 A1–A8 (anchored house pattern, line ranges);
`wt/scout-m11u3b-1` `22eaaba:scout_m11u3b_probe.py` (runnable fake-seam + parameterized-walker probes).

## Ported house pattern (anchors)

| mechanism | source anchor |
| --- | --- |
| deterministic content vs measured timing | `tests/eval_models.py:173-178,529-553`; measurements sibling `:1494-1503` `excluded_from_deterministic_equality=true` |
| content-addressed detail install | `tests/eval_models.py:1508-1539` (`<id>-<sha[:16]>.jsonl`, collision check) |
| aggregate written last, atomically | `tests/eval_models.py:1542-1560` → `write_atomic` `tests/fetch_real_clips.py:172-185` |
| automatic resume (3 journals) | `tests/eval_streaming.py:656-846` — `.part` + `.measurements.part` + `.resume.json` |
| CLI surface | `tests/eval_models.py:2019-2041`; `tests/eval_streaming.py:1541-1571` |
| structural hashing | `tests/eval_models.py:1002-1038` (`_structural_dump`, docstring-stripped `ast.dump`) |

## Acceptance predicates

### P1 — VAC contract fingerprint, rooted separately from the sherpa hash

- **P1.1** `VAC_CONTRACT_ROOTS` = exactly `ASR_DEVICE`, `VAC_CHUNK_S`, `VAC_TRIM_S`, `WHISPER_ENGINES`,
  `WhisperEngine`, `_vac_segments`, `OPENVINO_CACHE_DIR`, all rooted in `live_stt`.
- **P1.2** The closure is **qualified** (`module`, `name`) and follows `ImportFrom` edges, so it spans
  `live_stt.py` **and** `streaming.py`; it contains `streaming.StreamingProcessor` and `streaming.Segment`.
- **P1.3** An edit to a VAC surface moves `vac_contract_sha256` — proved on `streaming.py`
  (`HARD_TRIM_S`) and on a `live_stt` VAC constant.
- **P1.4** An edit to a sherpa-only surface **holds** `vac_contract_sha256` — proved on
  `live_stt.DECODE_CHUNK_S`.
- **P1.5** `asr_contract_sha256` is **frozen** across this unit:
  `sha256(asr_contract_source()) == aff253181730b72354ca34042b18d1cc1e54f0b1f7931c1721d599f91c2192ed`,
  `len(ASR_CONTRACT_SEEDS) == 20`, `len(ASR_CONTRACT_CUTS) == 17`, `len(asr_contract_closure()) == 25`.
  Rationale (L-030, map A8): the sherpa hash is deliberately the offline-row contract; dissolving its
  cuts into one hash recreates the false-refusal class `d936cbe` fixed.
- **P1.6** Hashing is docstring-stripped `ast.dump`, so a comment/docstring/`ruff format` rewrap is
  inert while every executed expression moves the hash.
- **P1.7** A root or reached dependency that does not exist raises; it is never silently dropped.

### P2 — Schema + row identity

- **P2.1** `VAC_SCHEMA_VERSION` is the VAC evaluator's own version, independent of
  `eval_models.SCHEMA_VERSION`; `eval_streaming`'s event/accounting contract is not reused (map A2).
- **P2.2** Canonical row identity = `arm_id/corpus_id`; the case list is ordered **arm-major** and
  validated as an exact ordered set — a duplicate `row_id` raises.
- **P2.3** Row rejection is structural and exhaustive: non-dict row, `row_id` mismatched against the
  case at that index, wrong `schema_version`, missing required field, non-finite / negative /
  `bool`-typed measurement, and `wall_seconds < decode_seconds` each reject.
- **P2.4** Deterministic content and measured timing are separate journals; the manifest publishes
  timing under a sibling marked `excluded_from_deterministic_equality=true`.

### P3 — Automatic resume (no `--resume` flag exists)

- **P3.1** Resume identity mismatch resets every artifact (detail, both `.part` journals, state).
  Identity includes `schema_version`, the ordered `row_id` list, and `vac_contract_sha256`.
- **P3.2** A truncated final line (interrupted append) reconciles to the last complete row.
- **P3.3** A crash **between paired fsyncs** (detail = N+1 rows, measurements = N) reconciles to N and
  re-decodes exactly one row.
- **P3.4** A complete prior run reuses every row and re-decodes zero; `rows_reused_on_resume` reports it.
- **P3.5** A structurally invalid row mid-prefix truncates from that row onward.
- **P3.6** Detail reaches stable storage before its measurement (write→flush→fsync each, detail first).

### P4 — Detail install + atomic aggregate

- **P4.1** Details install content-addressed as `<arm_id>-<sha256[:16]>.jsonl`; a staged file whose
  hash changed before install raises.
- **P4.2** An existing destination with a different hash raises (collision); an identical hash is a
  no-op re-install.
- **P4.3** All validation precedes any write, and the aggregate is written **last** through
  `write_atomic`, so an interruption can only leave an unreferenced immutable detail — never a torn
  or partially-referenced baseline.

### P5 — `--aggregate-only`

- **P5.1** Rebuilds the baseline **byte-identically twice** from cached details on the synthetic corpus.
- **P5.2** Refuses when the recorded `vac_contract_sha256` differs from the current one.
- **P5.3** Refuses when a referenced detail is missing or its hash does not match the manifest.

### P6 — CLI surface (house contract, map A5)

- **P6.1** Public surface = default full run + `--aggregate-only`. `--worker`, `--details`, `--summary`
  are `argparse.SUPPRESS`.
- **P6.2** `--worker` with `--aggregate-only` errors; `--worker` requires both `--details` and
  `--summary`; bare `--details`/`--summary` without `--worker` errors.
- **P6.3** No `--out`, no cache-dir flag, no corpus-path flag, no `--resume`. Corpora and arms are
  committed constants; resume is implicit.

### P7 — Hardware independence + gate identity

- **P7.1** The whole suite runs with no `models/`, no OpenVINO, no NPU farm, and no `.scratch/` tree.
- **P7.2** `gate.py` stays **7/7 blocking**; `tests/test_eval_vac.py` runs inside the existing pytest
  step; `ruff`, `ruff format` and both pyright passes stay clean.

## Invariant surfaces

- `tests/eval_vac.py` — new. `tests/test_eval_vac.py` — new.
- `tests/eval_models.py` — **parameterization only**: the single-module, ASR-hard-wired walker
  (`_live_stt_symbols`, `asr_contract_closure`) generalizes to a module-parameterized, `ImportFrom`-following
  qualified walker that both contracts call. Output for the ASR contract must be byte-identical (P1.5).
- `tests/test_eval_models.py` — may gain locks; its `reachable == contract | cuts` lock must stay green.
- `gate.py`, `live_stt.py`, `streaming.py`, `replay.py` — **unchanged**. This unit ships no production change.

## Probe-corpus seed

Synthetic corpus = 2 arms × 3 corpora = 6 rows, arm-major (`arm-a/corpus-1` … `arm-b/corpus-3`).
Content is a deterministic function of the case, supplied either by the fake decode callable or by
the real `WhisperEngine`/`_vac_segments` over the fake binding, whose `generate()` returns text as a
function of sample count (scout probe `probe_s1`, 3 rows green).

## Ruling record

- **R-1 — arm decoder wiring.** The kernel resolves an arm's decoder through a registry; the one
  shipped arm calls production `replay.replay_recognizer(...)` (map A3: production replay is
  authoritative, no copied scratch policy). M11.3d adds comparator arms + metrics. A stub decoder
  would be a placeholder and is rejected.
- **R-2 — where the walker lives.** In `tests/eval_models.py`, the existing shared module
  (`eval_streaming.py` already does `import eval_models as shared`). One walker serving both contracts
  is what makes P1.5 provable by construction rather than by a parallel reimplementation.

## Verdict table — `test-m11u3b-1` phase 1, MAIN rulings

Diff-blind enumeration of 12 divergent contract readings; MAIN ruled every row and encoded
each as a probe in `tests/test_eval_vac.py`. Phase 2 (the independent red suite) was NOT
funded — MAIN hit its context reserve at implementation close.

| id | ruling | where it is locked |
| --- | --- | --- |
| R01 | ACCEPT: identity gate → longest valid prefix per journal → common N → truncate both before decode | `test_an_invalid_row_mid_prefix_truncates_from_there`, `test_a_crash_between_the_paired_fsyncs_redecodes_exactly_one_row` |
| R02 | ACCEPT: identity mismatch clears detail + both `.part` journals + state, never the installed cache or baseline | `test_a_resume_identity_mismatch_resets_every_journal` (3 params) |
| R03 | ACCEPT: dedupe the canonical `row_id` string, before any decode or mkdir | `test_a_duplicate_row_id_is_rejected`, `test_an_empty_corpus_fails_before_touching_the_filesystem` |
| R04 | ACCEPT: `wall == decode` is legal; only `wall < decode` rejects | `test_zero_cost_and_equal_wall_are_legal_measurements` |
| R05 | ACCEPT: `-0.0` legal, `bool` rejected, measurements restricted to finite `int|float` | `test_measurement_rejection_is_structural` (7 params) |
| R06 | ACCEPT: an empty case list fails before directory creation | `test_an_empty_corpus_fails_before_touching_the_filesystem` |
| R07 | ACCEPT: aggregate-only refuses without replacing the baseline | `test_aggregate_only_refuses_a_missing_detail`, `..._a_tampered_detail` (both assert baseline bytes unchanged) |
| R08 | ACCEPT: collision preflight precedes every write (inherited from `install_content_addressed_details`) | `test_a_content_addressed_collision_raises`, `test_the_aggregate_is_written_last` |
| R09 | ACCEPT: an alias resolves to the SOURCE symbol, not the local name | `test_an_aliased_import_resolves_to_its_source_symbol` |
| R10 | ACCEPT + code change: star and relative imports now RAISE in `module_surface` rather than silently escaping the closure | `test_an_import_form_that_hides_the_surface_fails_closed` (2 params) |
| R11 | ACCEPT: all 7 VAC roots are sherpa cuts and stay included; the two contracts are independent | `test_a_vac_root_is_included_even_though_the_sherpa_contract_cuts_it` |
| R12 | ACCEPT + probe change: rebuild identity is content, so the second rebuild runs after `os.utime(installed, (0, 0))` | `test_aggregate_only_rebuilds_byte_identically_twice` |

R10 is the only row that changed shipped code: a `from streaming import *` in `live_stt.py`
would have dropped the entire streaming surface out of the VAC closure with no error.

## Close record

- Gate **7/7 blocking pass**; **466 passed / 1 skipped** (was 382/1) — `tests/test_eval_vac.py` = 84 probes.
- `asr_contract_sha256` = `aff253181730b72354ca34042b18d1cc1e54f0b1f7931c1721d599f91c2192ed`, unchanged across the walker parameterization (P1.5 asserted in-suite).
- `vac_contract_sha256` = `56e017c3d9ad0a84852cb1dd5b3baf04b2e70420260f541ad63c35cc05e36bb1`; closure = 22 qualified symbols, 6 of them in `streaming.py`.
- MILESTONE-REVIEW dispatch inputs: this contract + table; branch tip `wt/scout-m11u3b-1` `22eaaba` (probe surface). No `wt/test-m11u3b-1` content — phase 2 unfunded, worktree removed.
