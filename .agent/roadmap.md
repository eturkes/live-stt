# Roadmap — live-stt

Canonical plan + status. Pick the lowest-numbered OPEN unit; restate its acceptance criteria before coding. Trajectory: single-file tool, simplicity over completeness — no frameworks, no config, no premature abstraction. Milestone states: UNPLANNED · IN-PROGRESS · IMPLEMENTED · REVIEWED · PARKED (skipped by MODE dispatch; the entry names its re-entry precondition, and meeting it + clearing the marker returns the milestone to the active-milestone scan). Unit states: OPEN · BLOCKED · DONE. Ledger dispositions: DEFERRED · SUPERSEDED · OUT-OF-SCOPE · REJECTED. Context records = `context-gauge` readings (machine tool, `~/.local/bin`), `N% NK/240K`: `main=` = MAIN, which now carries implementation, and `mate=` = peak teammate high-water; a unit closed at the context reserve records that overrun beside `main=` so PLANNING sizes against the real cost. Historical `impl=` = peak implementing teammate under the superseded delegated-implementation model ≈ implementation load, so PLANNING sizes a unit against the nearest analog's `main=` or `impl=` (M10.5a–d predate teammate resolution and record final-turn values, which understate peaks). Units carry `tier=kernel|data|docs` from planning; units planned without one default to `kernel`. A REVIEWED milestone keeps one summary here — status, commit range, shipped surface, gauge band, archive pointer — while its unit detail moves to the named `.agent/archive/<record>.md`, outside the attached set and read on demand.

## Status
- Milestone: **M11 production-qualify the shipped whisper+VAC+NPU path** — IN-PROGRESS (planned 2026-09-01).
- Trigger: M10 closed by events. D-016 shipped whisper large-v3-turbo int8 on OpenVINO NPU under a VAC policy as the default engine, answering M10's question outside its machinery. That choice is made and is NOT reopened here; what is missing is everything that makes it defensible — the default engine does not install from `uv sync`, the pyright gate is red and was reported green, D-016's numbers exist only in gitignored `.scratch/`, the real-time guarantee does not cover the VAC branch, and the shipped surface is largely untested.
- Current architecture (D-009 + D-016): mic → resample → 2 s `AudioQueue` → silero VAD → VAC controller (speech-start opens a LocalAgreement-2 buffer in `streaming.py`; every `VAC_CHUNK_S`=1 s re-decodes the open buffer and commits what two decodes agree on; speech-end flushes the tail) → whisper large-v3-turbo int8 on OpenVINO NPU → `CodexTranslator` (JA→EN via persistent `codex app-server`, D-011) → `emit_line`; partials render on the meter status line, one utterance = one numbered JA line = one translation turn. `--engine k2v2|parakeet` retains the M9 sherpa fallback (VAD-segment close → ring pre-pad → 8-segment queue → low-RMS chunked decode >10 s, D-010). Degrades to JA-only when codex is absent/failing.
- Agent-covered: VAC buffer mechanics + the VAC loop over stubbed VAD/recogniser; `SessionContext` learning rules; two-engine short goldens + CER stressors + 4:48 narration **on the sherpa fallback only**; deterministic 44.722 s paced backpressure **on the sherpa fallback only**; shutdown/stage-failure/translation-degradation mechanics (`.agent/memory.md` § Smoke).
- User-only debt: 2026-07-02 partial pass covered devices/capture/device-select/Ctrl+C-shutdown observables; the second host run confirmed EN 1–3 trailing JA 1–3. Latency feel, `-o`, soak, sustained live cadence, Ctrl+C-mid-decode, and the whole VAC partial-caption cadence remain (`.agent/memory.md` § Smoke, L-004).
- **Carried from M10, unfixed — the ASR pipeline fingerprint is whole-file.** `tests/eval_models.py` `pipeline_fingerprint()` hashes all of `live_stt.py`, so `--aggregate-only` refuses to rebuild the committed `model_baseline.json` against HEAD; the whisper/VAC/context commits widened the drift further. Substance of the decoded ASR evidence is unaffected — the drift is outside the offline ASR row path — but reproducibility is gone and every production edit re-breaks it. Full defect record + the audited minimal repair + the explicitly rejected repairs → `.agent/archive/m10-asr-tournament.md`.

## Open (do these; lowest ID first)

### M11 — production-qualify the shipped whisper+VAC+NPU path

Outcome: make the default STT path installable, packaged, gated, measured from committed state, and honest about its real-time guarantee. M10 chose the engine; M11 makes that choice maintainable. Engine/model selection is fixed by D-016 — units here qualify install, packaging, evidence, timing, tests, and docs against that fixed choice.

Planning evidence + scope boundary:

- **The quality gate is red and was reported green.** pyright over its own declared files yields exactly 2 errors: `live_stt.py:315` unresolved `openvino_genai` and `live_stt.py:1181` `insert_audio` on Optional `processor` (runtime-safe behind `elif speaking:`, unprovable to pyright). `streaming.py` is a production module absent from the pyright arg list and adds exactly 1 more, `streaming.py:84` "Object of type `object` is not callable". Commits `1cc241a`/`23e8fa8`/`b633b0c`/`76d986d` each reported "Gate N passed / 1 skipped, ruff clean" = pytest + Ruff only. pytest (198 passed / 1 skipped) and Ruff are green; `ruff` is not on `PATH` and needs `uv run`.
- **The default engine neither installs nor packages.** `openvino`/`openvino-genai` sit in the optional `whisper` extra (`pyproject.toml:26-32`) and are absent from `.venv`, so plain `uv sync` leaves the *default* `--engine whisper` unrunnable; a clean-`PYTHONPATH` `.venv` resolves `openvino_genai` to `None` while NumPy resolves. Separately, `live_stt.py:30` imports `streaming`, but `pyproject.toml:48` sets `only-include = ["live_stt.py"]` and `:51` lists the same single module for the sdist — **`streaming.py` ships in neither artifact, so a built distribution is broken** while an editable install hides it. `pyproject.toml:4` still describes the tool as local sherpa-onnx. Inherited `PYTHONPATH` additionally shadows any correct wheel with the host OpenVINO build that cannot execute in-container (needs GLIBC 2.43, container has 2.41), surfacing as `ImportError: cannot import name 'AxisSet'` rather than a clean missing-module error.
- **D-016's evidence is scratch-only, and one headline number has no artifact at all.** The shipped-path numbers live in gitignored `.scratch/policy_*.json`; committed evaluators (`tests/eval_cer.py`, `tests/gen_replay_goldens.py`) score the sherpa engines only. Worse, **retention CER 0.0583 appears in no scratch artifact**: `.scratch/policy_retention_vac_npu.json` and `.scratch/policy_results.md` both retain `I=12` and CER `0.0686106`, and only `23e8fa8`'s commit body records the append-only rerun that reached 0.0583. That number must be re-derived or D-016 corrected. `CLAUDE.md` Engineering requires a gate backing a durable claim to rerun from committed state.
- **The real-time guarantee does not cover VAC.** `tests/eval_backpressure.py:116-196` forces legacy dispatch (`worker(object(), …)`), charges each decode `closed_segment × RTF` at default RTF 0.20 (`:60-61`), and asserts a nonzero segment queue (`:253-268`); VAC has no segment queue and re-decodes an open, growing, trimmed buffer every 1 s, so the evaluator cannot represent or run that branch. VAC awaits each decode inside the coroutine draining `audio_q` (`live_stt.py:1122`, `:1149-1187`, `:1196-1223`), so VAD feeding pauses during decode while capture buffers into 2 s only (`live_stt.py:37`, `:492-518`). `README.md:80` claims the opposite: "Capture and VAD feeding continue during decode." Aggregate RTF 0.48-0.60 proves mean compute < real time, not bounded maximum blockage. Drop-freedom on the shipped path is **unmeasured, not disproved**. `streaming.py:143-157`'s force trim can additionally discard un-emitted text, and `_vac_segments` surfaces no counter for it.
- **The shipped surface is largely untested.** No committed test covers engine/device routing (`live_stt.py:46-59`), `WhisperEngine` construction/decode/hotword capability (`:305-354`), the `check_models` whisper marker (`:458-474`), status-line partials (`:1124-1128`, `:1227-1255`), the three live CLI flags (`:1382-1414`), `worker`'s duck-typed dispatch, or exactly-once translator/context publication (`:1133-1143`, `:1209-1213`). `VAC_CHUNK_S`/`VAC_TRIM_S` (`:65-66`) bind to no committed measurement, so changing either leaves the suite green while invalidating D-016. `replay.py:189` defaults to k2v2 but accepts explicit `--engine whisper` and then drives VAC; it is the committed golden matrix that is sherpa-only (`tests/gen_replay_goldens.py:36-48`), and `replay.py:184-192` still tells users k2v2 "matches live-stt default".
- **D-016's declared limits stand and are not re-litigated:** the hotwords gain used an ORACLE term list drawn from the reference (passive learning has contributed 0 terms unseeded); long_form absolute CER is inflated by period-vs-modern orthography in the 「ごん狐」 reference; one clip per corpus. Energy per watt needs host access (RAPL `energy_uj` is mode 400 `nobody:nogroup`, unreadable in-container even under sudo) and stays out of scope.
- Carried from M10: the whole-file ASR pipeline fingerprint keeps `tests/eval_models.py --aggregate-only` red; M11.3 owns the repair.
- Out of scope: reopening engine/model selection; NPU applicability or ports for the sherpa fallbacks (M10.6/M10.7a/M10.7b, retired); EN-rendering learning (`polish.md` P-002); energy per watt.

- **M11.1 — Installable, packaged, gated default path [OPEN]. `tier=kernel`.** Gates every later unit, because none can run a whisper evaluator until it lands. Decide from install evidence whether `openvino`/`openvino-genai` stay an extra or become hard dependencies, and make the chosen route work from a clean checkout. Add `streaming.py` to the wheel and sdist; refresh the `pyproject.toml:4` description to name the Whisper/OpenVINO default while keeping the sherpa fallback. Resolve all three pyright errors and add `streaming.py` to the pyright arg list. Commit the gate runner — this absorbs `polish.md` P-001, which stops being optional the moment M11 claims a restored gate — so one committed script owns the exact step set and file list. Correct `replay.py`'s stale default claim + module usage, and record the `PYTHONPATH`-clearing requirement wherever a command touches the whisper engine. Acceptance: the committed gate runner exits 0 on a clean checkout and exits nonzero under a seeded failure in **each** blocking step (one test per step, so the runner is non-vacuous); a wheel built and installed into an isolated environment lists `streaming.py` as a member and satisfies `import live_stt` plus `live-stt --help`; after the documented install a one-clip whisper decode succeeds in-container; no production behavior change beyond installability.
- **M11.2 — Shipped-path unit coverage [OPEN; after M11.1]. `tier=kernel`.** Runs parallel to M11.3/M11.4 rather than behind them. Close the untested-surface list: engine/device routing, `WhisperEngine` construction/decode/hotword capability including the NPU drop → empty `biased` integration, the `check_models` whisper marker, status-line partial rendering, the three live CLI flags, `worker`'s duck-typed dispatch, and exactly-once translator/context publication per utterance. This supersedes the `## Rejected` entry "Assert `check_models` missing-file branch" — that rejection stands for a tautological literal assertion and is replaced by an engine→marker **routing** test with a real failure mode. Acceptance: mutating the default engine string, the default device, the hotword-capable device set, the whisper marker filename, or the duck-dispatch predicate each fails at least one committed test — behavior-level mutations, not literal echoes; no test requires a microphone, a model download, or a working OpenVINO install.
- **M11.3 — VAC evaluator machinery + fingerprint repair [OPEN; after M11.1]. `tier=kernel`.** Machinery only — no committed real baseline, so the judgment-bearing code lands before any multi-hour run. Port the `.scratch/` policy harness into a committed evaluator following the house pattern in `tests/eval_models.py`/`tests/eval_streaming.py`: deterministic content separated from timing/RSS measurement, content-addressed detail, atomic aggregate. Ship a **machine-readable inventory of every D-016 claim** so an omitted metric fails a validator instead of yielding a byte-stable but incomplete baseline. Repair the carried-forward fingerprint in the same unit: narrow `pipeline_fingerprint()` to a dependency-scoped ASR/scorer/replay contract, prove that an irrelevant `live_stt.py` edit does not invalidate it while a decode-input edit does, and restore byte-exact `--aggregate-only`. Add at least one committed Whisper/VAC replay characterization row with a regeneration lock, closing the sherpa-only golden matrix. Acceptance: a synthetic multi-row corpus proves aggregation, schema, resume/atomicity, and validator behaviour without hardware; the claim-inventory validator exits nonzero when any D-016 metric is absent; `tests/eval_models.py --aggregate-only` reproduces `model_baseline.json` byte-exact; the whisper golden regenerates byte-identically twice. **Source inventory — `.scratch/` must not be cleaned before this unit runs**, because it holds the only copy of harness and results. Producers: `streaming_policy.py`, `policy_vac.py`, `policy_baseline.py`, `policy_corpus.py`, `compare_policies.py`, `decode_cost_matrix.py`, `long_form_whisper_eval.py`, `whisper_eval.py`, `whisper_probe.py`, `vad_real_compare.py`, `prompt_hotword_matrix.py`, `npu_asr_prompt_hotword_matrix.py`. Results: `policy_results.md`, ~30 `policy_*.json` / `lf_*.json` / `long_form_*.json` / `model_{a,b}_*_eval.json`, `policy_arms.log`, `hotwords.log`, `decode_cost_matrix.json`. Three layers must stay separate when porting — VAD/VAC policy runs (the shipped comparison), prompt-conditioning matrices (the deleted prev-text carry + the oracle hotwords probe), and earlier whole-file Whisper/Kotoba device probes (superseded harness behaviour the port must not canonize). Scripts predate `b633b0c`, so verify each imports before trusting it.
- **M11.4 — VAC real-time qualification [OPEN; after M11.3]. `tier=kernel`.** Deliberately precedes the baseline run, because this is the unit that can force a production design change and a redesign would obsolete a baseline measured before it. Consume M11.3's trace harness, not a committed baseline. Add a VAC scenario to the backpressure evaluator with a `decode_segments` recogniser, real silero, production cadence/trim, and per-update cost driven by an empirical NPU `(buffer_s, decode_s)` trace; replace the legacy `segment_depth>0` assertion with VAC invariants. Surface `streaming.py`'s force-trim counter and rule it. Acceptance: the 44.722 s continuous stressor and the 182 s retention clip both run paced at 20 ms with `state.dropped == 0`, committed queue high-water and maximum-contiguous-decode bounds, and asserted speech-open + VAC dispatch + decode/update counts + exact trace consumption + all-sample/EOF accounting + zero segment-queue use; a deliberately overloading trace produces a nonzero drop, proving the check is not vacuously green; `forced_trims == 0` or a committed threshold with its loss quantified. `README.md:80` is corrected to whatever the measurement shows. **A design change decoupling VAC decode from `audio_q` draining reaches the user before implementation.**
- **M11.5 — D-016 evidence run + committed baseline [OPEN; after M11.4]. `tier=data`.** Data tier: one structural validator plus a live spot-check, defects fix forward — the assurance already sits in M11.3's kernel-tier machinery. Run the two pinned corpora (`tests/long_form.json` 4:48 narration, `tests/retention_probe.json` 182 s pause-free) plus the k2v2 comparator through that machinery and commit the baseline. Bind `VAC_CHUNK_S`/`VAC_TRIM_S` to the measurements they were chosen from. Reconcile every D-016 number against the claim inventory, starting from the known gap: retention 0.0583 has no artifact and either re-derives or corrects D-016. Acceptance: two rebuilds byte-identical; the claim inventory fully populated with no metric left unmeasured; every divergence from D-016 recorded as an explicit correction, never a silent baseline rewrite; the baseline records normalized `EXECUTION_DEVICES` and fails unless the shipped run actually placed on `NPU`; a committed test fails when baseline and shipped constants disagree.
- **M11.6 — Docs + smoke realign [OPEN; after M11.4]. `tier=docs`.** Bring `README.md`, `models/README.md`, and the § Smoke checklist onto the shipped path: install command, VAC cadence and partials, corrected backpressure wording, whisper model acquisition, and the user-only verification items the new path creates. Acceptance: no human-facing surface describes the retired k2v2/VAD default as current; every claim traces to a committed measurement or is marked unverified (L-004); `grep -nP '[\x{2013}\x{2014}]' README.md` stays clean (L-021); a committed claim→source inventory traces every human-facing measurement to a committed baseline row or marks it unverified (L-004), so the docs-tier close adjudicates each named claim instead of eyeballing. Also rule the counter-scope question the `## Out of scope` line leaves open: correctness-failure telemetry (force-trim loss) may use the existing meter/log surface, or stays evaluator-only with `forced_trims == 0` required — that exclusion covers metrics dashboards, not correctness counters.



## Done (ID · outcome · decisions/lessons produced)
- **T1–T8 · pre-milestone build-out [CLOSED].** `8ec8482..e3a654c`. Shipped: timestamped `-o` output, `--list-devices`/`--device`, partial-turn shutdown flush, structured logging, the `.githooks/` pytest hook (D-007), the sherpa-onnx + silero local STT leg replacing Gemini (D-009/D-010), `CodexTranslator` over a persistent `codex app-server` (D-011), deterministic `replay.py` + engine-first goldens (D-014), the 7-clip Common Voice corpus (L-017), and T8's translator degradation net — 3-strike disable, backlog evict + `tdrop=`, EOF turn-wake, dead-server refusal after warm-up (L-022). Produced D-002/D-005/D-006/D-007/D-009/D-010/D-011/D-014 + L-001/L-004/L-005/L-010/L-016/L-017/L-018/L-019/L-020/L-022. Gauge band: none recorded — predates context records. Detail → `.agent/archive/t-series-buildout.md`.
- **M9 — agent-runnable accuracy + long-form completeness [REVIEWED].** `e74294c..b2428bd`, 23 commits. Shipped: `cer.py` NFKC/S-D-I scorer + the crossfade stressor builder, `tests/eval_cer.py` + `cer_baseline.json`, virtual-clock `tests/eval_backpressure.py`, low-RMS decode chunking for >10 s segments, the seconds-bounded two-stage `AudioQueue`/`TaskGroup` worker, and the pinned 4:48 LibriVox/Kokoro/Aozora long-form corpus. Closing numbers: k2v2 excess deletion 17.02 %/28.03 % → 0.0 % on both stressors; 4:48 strict CER k2v2 25.38 %, parakeet 23.50 %; paced backpressure drop=139 → 0 at max audio_q 0.020/2.000 s; 84 tests green. Review accepted four cross-unit findings (shutdown ordering, both-engine 15 % total-CER cap, revision + SHA-256-pinned Common Voice regeneration, honest crossfade/resample wording). Produced L-023/L-024 + the M9.4 re-affirmation of D-010; real mic/OS-signal/Codex-cadence/soak paths stay user-only (L-004). Gauge band: none recorded — predates context records. Detail → `.agent/archive/m9-accuracy-longform.md`.
- **M10 — evidence-backed Japanese ASR model/architecture tournament [REVIEWED].** `6c20bd6^..bf7e39c`, 14 M10-traced commits (`git log --grep "(M10[. ]"`; the range interleaves unrelated translator/ops commits, so the grep is the selector). Closed **by events on user decision (2026-09-01)**, not by running its tail: D-016 shipped whisper large-v3-turbo int8 on OpenVINO NPU under a VAC policy as the default engine, answering M10's outcome question outside its machinery and breaking M10's own standing constraint (*preserve `k2v2` as the shipped default*). Shipped surface: the pinned full JA corpus (`tests/fetch_real_clips.py` + `short_corpus.json` — Common Voice 8 test 4,483 rows / 6.484 h + FLEURS JA test 650 rows / 2.364 h), the model-neutral evaluator + control baseline (`tests/eval_models.py` + `model_baseline.json`), fail-closed candidate acquisition (`tests/fetch_eval_models.py`, `tests/fetch_eval_streaming.py`), the direct-streaming evaluator + schema-2 full-corpus/resumable machinery (`tests/eval_streaming.py` + `streaming_baseline.json`), the pause-free retention probe (`tests/build_retention_probe.py` + `retention_probe.json`), and the sherpa-onnx 1.13.4 runtime lock. Measured conclusions: parakeet displaced k2v2 as comparator on Common Voice micro CER 8.426 % vs 8.953 %; Qwen3-ASR (15.552 %) and Cohere Transcribe (10.565 %) failed content **and** CPU-resource gates; no Nemotron streaming variant qualified (560 ms eliminated on measured gates, 80/160/1120 ms unrun). Retired unrun and superseded by D-016: **M10.6** NPU applicability matrix, **M10.7a/M10.7b** sherpa direct-NPU ports; **M10.8**'s one live deliverable — amending D-010 — is discharged in this close commit, while its clean-process finalist rerun and fingerprint audit were not performed because the finalist is no longer a sherpa model. Not performed: the milestone-level cross-cutting review and the `audit` claim-replay pass — units M10.1–M10.5e carry per-unit adversarial review only, so cross-unit integration is unreviewed. Carried forward to M11: the whole-file ASR pipeline-fingerprint defect (`eval_models.py --aggregate-only` refuses to rebuild `model_baseline.json` against HEAD) and D-016's unported scratch-only evidence. Gauge band: recorded for M10.5a–d only — main 21–83 %, impl 45–47 %. Detail → `.agent/archive/m10-asr-tournament.md`.

- **T-XLAT-ATTRIB-001 · cascade attribution [CLOSED] — speech→JA is the fault, not JA→EN.** `984fea5`. 5 arms × 321 unique FLEURS sentences through the real `CodexTranslator` on production model/effort/tier/instructions, one isolated `codex app-server` per arm, 2 judges (`gpt-5.6-sol`/`gpt-5.6-terra` @high) + human FLEURS EN as an unlabelled 6th candidate. Attribution of total adequacy loss: **ASR content 78 %, VAD fragmentation 16 %, punctuation 5 %**; end-to-end vs ceiling +0.997 [+0.858, +1.132]; live-arm severe failures (`adequacy≤2`) 13.4 % vs 0.0 % oracle. Judge reliability r=0.881, identical arm ordering from both judges, 0 failures. Produced L-028 + the D-010 engine evidence behind the pending default-engine decision below. Gauge band: not recorded. Detail → `.agent/archive/t-xlat-attrib-001-cascade.md`.

- **T-SAVE-DEFAULT-001 · transcripts persist by default [CLOSED].** User directive: save every transcription/translation to the device, gitignored in-repo. Shipped: `TRANSCRIPT_DIR` = gitignored `transcripts/`, one file per run named by local start time (`%Y-%m-%dT%H-%M-%S.txt`, colon-free); `transcript_path()` resolves `--no-save` → None, `-o PATH` → that path, else the session file; `TranscriptFile` mkdirs the parent eagerly (missing `-o` dir fails at startup) and defers file creation to the first line (a silent session leaves no empty file); `-o`/`--no-save` are an argparse mutually-exclusive group. `emit_line`'s format, flush-per-event, and the decode path are unchanged (user chose per-session files over a shared append log, so `n` numbering stays self-consistent per file). 5 model-free locks in `tests/test_audio.py` (default/override/off resolution, lazy creation, append+flush). Gate: 158 passed/1 skipped (the pre-existing deferred schema-2 streaming lock); Ruff check clean; `ruff format --check` red only in the pre-existing hand-laid-out hunks of both touched files (HEAD baseline red on the same hunks, none in new code); Pyright prod + tests 0/0/0; offline import ok. Not verified (L-004): mic-session wiring of the startup `Transcript:` line and the `finally` close — smoke item 8 now covers default/`-o`/`--no-save`/silent-session. Gauge band: main=51% (122K/240K).

## Rejected (recorded so they are not re-litigated)
T8 screen:
- GitHub Actions CI mirroring the local hook — ~zero marginal catch for a single-user/AI-only repo; the one novel hazard (moved-dir venv shebangs) is absent from CI's fresh-clone env; D-007 already chose the minimal local hook (L-019 productivity theater).
- Drain residual codex notes at turn entry — self-contradicting: the next turn's collect loop already drains leaked notes; worst case is one self-healing dropped EN line.
- Assert `check_models` missing-file branch — tautological change-detector against a hardcoded literal; a regression fails loudly on next manual run.
- Bound `_notes` queue (DoS) — incoherent threat model: flooding needs the locally-spawned, user-authenticated codex CLI to attack its own client, already trusted.
- CodexTranslator thread-rotation unit test — tautological (asserts the modulo in the test body, not the live code).

T7 screen: merge `submit`/`submit_sentinel` (deliberate drop-vs-must-land split); cross-file dedup of WAV loaders/writers (different formats/sources); centralize the triplicated `CACHE` constant; prune overlapping resample tests.

## Deferred
- T2.2 — Parameterize source language. Tool is Japanese-only by design (user-confirmed). Revisit if the use-case expands: add `--language`, template into the system instructions, dynamic 2-letter ISO prefix in `emit_line`, extend `test_emit_*`. Approach stays valid; doesn't pay rent today.
- M10 candidate-screen remainder: old multilingual streaming zipformer + SenseVoice lack current JA evidence against the shortlisted 2026 models; Moonshine-JA is compact (~62 MB) but its source card reports a nonstandard/unclear license and no persuasive JA evaluation; ReazonSpeech-k2-v2 2025 is JA-specialized but adds PyTorch/Transformers + remote custom model code instead of reusing sherpa. Re-open only if M10's shortlisted candidates all fail and the added provenance/runtime surface can buy a materially different hypothesis.

## Superseded
- T-BACKENDS-001 — Backends spike, superseded by the T4 series (user ruled out metered API keys, removing Deepgram + OpenAI Realtime candidates). The backends spike report is pruned (git history, D-005); its "populate keys" recommendation is void.

## Out of scope (do not redebate)
Config files / YAML / TOML for tunables (constants at the top of `live_stt.py` are the config surface) · multi-mic mixing · speaker diarization · web UI · auth / multi-user · metrics dashboards beyond the backlog/drop status counters · package split / multi-module layout (`.agent/memory.md` D-002).

## Decisions pending from user

*(none)*

(Last resolved: **M10 disposition + D-016 evidence weight** — with M10's tail overtaken by D-016 (whisper/NPU/VAC shipped as default, breaking M10's own "preserve k2v2" constraint), offered (a) re-plan M10's tail, (b) run M10.6 as written, or (c) close M10 and open a fresh milestone; the user chose **(c)** on 2026-09-01, and separately ruled the D-016 evidence port **spine work** rather than a polish row. M10 is REVIEWED-by-events with M10.6/M10.7a/M10.7b retired unrun, and M11 owns installability, gating, the evidence port, and the VAC real-time question. Before that: **ASR device** — offered NPU (identical unconditioned CER, 1.7-2.1x headroom, no ASR-side term biasing) vs GPU (unlocks `hotwords`, 0.2321 -> 0.1858 on proper-noun-heavy speech), the user chose **NPU** on 2026-08-31, shipping D-016: whisper large-v3-turbo + VAC streaming policy as the default STT path, prose carry deleted, session context reaching the model through the translator glossary. This also retires the older **k2v2 -> parakeet default** question: both sherpa engines are now non-default fallbacks behind `--engine`, and parakeet's M10.3/T-XLAT-ATTRIB-001 advantage over k2v2 no longer decides what ships. Before that: **how the streaming leg closes** — offered (a) close on existing evidence or (b) one bounded ~2 h FLEURS falsification first, the user chose **(a)** on 2026-08-03, closing M10.5e + M10.7e; D-016 reopened and settled that leg on the policy axis M10.5 never tested. Before that: the two `TRANSLATOR_INSTRUCTIONS` defect lines — generic drug names + never invent a patient's sex — user-approved 2026-08-03 on a 351-turn 3-arm clinical probe, evidence folded into D-011. Before that: translation leg -> `gpt-5.6-luna`+`low`.)
